apiVersion: v1
kind: Service
metadata:
  name: sre-sample-app
  namespace: default
  labels:
    app: sre-sample-app
spec:
  selector:
    app: sre-sample-app
  ports:
    - port: 80
      targetPort: 5000
  type: ClusterIP